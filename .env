DATABASE_URL=postgresql://<USER>:<PASSWORD>@<HOST>:5432/<DBNAME>
PGSSL=require        # set to require for Neon/Render/Supabase; remove for local Postgres
PORT=3001
CRON_SCHEDULE=*/5 * * * *    # every 5 minutes